# Micro Scene Exports

Save exported micro-scene `.md` files here.

These are tiny animated cutaway notes: postman arrivals, research jumps, gate interruptions, walks, tree stops, fetch beats, dog treats, tail-sniff checks, grooming resets, ferry watches and similar short frames.

Use the builder at `builders/micro-scenes.html`.
